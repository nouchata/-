export type HideDisplayData = {
	/* also hides chat button where viewport in mobile width */
	hideSidebar?: boolean;
	/* hides play button & message button */
	hideButtons?: boolean;
	/* remove margin & padding for the main container */
	hideMainContainerStyle?: boolean;
};